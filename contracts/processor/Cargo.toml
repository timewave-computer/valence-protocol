[package]
name = "processor"
version = { workspace = true }
edition = { workspace = true }
authors = { workspace = true }
description = "CosmWasm Smart Contract that executes the messages on each service/account of the workflow by iterating over a queue"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
cosmwasm-std.workspace = true
